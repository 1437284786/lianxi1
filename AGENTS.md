# 国际营销项目协作规则

本文件适用于仓库根目录及全部子目录。后续 Codex 任务开始前，必须依次读取本文件、[`PROJECT_STATUS.md`](PROJECT_STATUS.md)、[`ROADMAP.md`](ROADMAP.md)、[`DECISIONS.md`](DECISIONS.md) 和 [`DATA_POLICY.md`](DATA_POLICY.md)，再读取本次任务涉及模块的 README、数据文件和缺失信息清单。

## 1. 项目目标

这是一个长期棕刚玉国际营销项目。项目目标包括：

- 产品类别定位；
- 应用行业与客户画像；
- 竞争对手分析；
- 目标国家筛选；
- 市场进入优先级；
- 独立站规划；
- SEO；
- Google Ads；
- 社交媒体；
- B2B 平台；
- 邮件开发；
- 内容营销；
- 线索管理；
- 数据分析；
- 推广预算；
- 持续复盘和优化。

各项工作必须按 [`ROADMAP.md`](ROADMAP.md) 的依赖关系推进，不能用后续营销产出倒推或编造尚未确认的产品能力。

## 2. 当前产品

### 当前生产、当前可销售产品

- 一级棕刚玉；
- 二级棕刚玉。

### 未来计划产品

- 煅烧棕刚玉；
- 镀铱刚玉、镀衣刚玉或其他表面处理棕刚玉。

未来产品必须与当前可销售产品分开处理。未来产品不得进入当前产品目录、当前供货能力、报价、交期、广告、销售承诺或客户推荐；只有完成技术、生产和商业批准后，才能调整其生命周期状态。

## 3. 数据优先级

数据可信度从高到低：

1. 企业内部已确认数据；
2. 企业检测报告、COA、TDS、SDS；
3. 标准机构、政府、行业协会；
4. 企业官方网站和官方技术资料；
5. 经销商和行业媒体；
6. B2B 平台及其他低可信公开资料。

不得用公开行业数据替代企业真实数据。公开资料只能用于行业参考、研究假设、对比框架或待验证问题，具体规则见 [`DATA_POLICY.md`](DATA_POLICY.md)。

## 4. 数据状态

数据记录只允许使用以下状态值：

- `confirmed`
- `pending`
- `reference_only`
- `not_applicable`
- `unverified`
- `conflicting`

使用规则：

- `confirmed`：只能用于企业内部已经核实，并且能追溯到批准人或受控文件的资料；
- `pending`：已经识别但仍待企业补充、核验或批准的数据；
- `reference_only`：行业、标准入口、竞争对手或其他公开参考数据，不代表本企业数据；
- `not_applicable`：经确认不适用于指定产品或场景的数据；
- `unverified`：有线索但无法独立验证、来源不可访问或证据不足的信息；
- `conflicting`：不同来源对同一事项存在实质冲突，且尚未完成裁决的信息。

不得自创近义状态或把决策状态、任务状态与上述数据状态混用。

## 5. 禁止事项

- 不得编造企业参数；
- 不得把竞争对手数据写成我方数据；
- 不得把一级、二级刚玉简单确定为 `First Grade` 和 `Second Grade` 作为最终国际产品名；
- 未确认实际工艺前，不得把镀铱刚玉正式翻译为 `Iridium-coated Corundum`；
- 不得删除已有研究资料；
- 不得覆盖其他模块；
- 不得创建虚假来源；
- 不得把行业常见值标记为 `confirmed`；
- 不得在未确认产品能力前编写夸大营销文案；
- 不得在当前阶段投放广告或建立真实广告账户。

## 6. 文件修改规则

- 每个任务尽量只创建或修改一个业务模块；
- 不得无关修改其他目录；
- 每次修改必须列出变更文件；
- 所有重要结论必须能够追溯到企业数据、受控文件或 `source_id`；
- 所有未知事项必须进入相应模块的缺失信息清单；当前产品缺失信息入口为 [`product-data/missing-information.md`](product-data/missing-information.md)，公开研究未决事项入口为 [`public-research/unresolved-questions.md`](public-research/unresolved-questions.md)；
- 每次任务完成后必须执行格式和一致性检查；
- 不得直接修改 `main` 分支，应通过任务分支和 Pull Request 提交；
- 修改项目阶段、关键依赖或优先级时，同步更新 [`PROJECT_STATUS.md`](PROJECT_STATUS.md) 和 [`ROADMAP.md`](ROADMAP.md)；
- 形成或更改关键项目决策时，同步更新 [`DECISIONS.md`](DECISIONS.md)；
- 修改企业产品主数据时，必须遵守 [`product-data/README.md`](product-data/README.md) 的维护流程和 [`DATA_POLICY.md`](DATA_POLICY.md) 的批准流程。

## 7. 输出语言

- 项目内部说明以中文为主；
- 产品英文名称、SEO 词和国际市场内容应保留英文；
- 重要术语同时提供中文和英文；
- 不使用模糊、夸张或无法证明的营销表述。

## 8. 任务完成检查

每次任务至少检查：

1. 改动是否处于路线图允许的当前阶段；
2. 当前产品与未来产品是否分开；
3. 企业数据与公开参考数据是否分开；
4. 数据状态是否属于允许枚举且使用正确；
5. 重要结论是否有企业证据或 `source_id`；
6. 未知事项是否进入缺失信息清单；
7. 内部链接、JSON、CSV 或 Markdown 格式是否有效；
8. 是否仅修改任务相关文件；
9. 是否列出检查命令、变更文件、当前阶段和下一项建议任务；
10. 是否已通过任务分支、提交和 Pull Request 交付。
